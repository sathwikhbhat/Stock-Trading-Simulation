# Server configuration
server.port=8080

# MongoDB configuration
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=stocksim
# If your MongoDB requires authentication, uncomment and set the following
# spring.data.mongodb.username=admin
# spring.data.mongodb.password=password

# Connection pool settings
spring.data.mongodb.auto-index-creation=true
spring.data.mongodb.uuid-representation=standard

# Improved logging for debugging
logging.level.com.stocksim=DEBUG
logging.level.org.springframework.data.mongodb=INFO
logging.level.org.mongodb=INFO

# Show MongoDB queries
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG

# Thymeleaf
spring.thymeleaf.cache=false

# Session timeout commented out until issue resolved
# server.servlet.session.timeout=1800s 